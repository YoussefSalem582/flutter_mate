# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# API Keys and Secrets - NEVER COMMIT THESE!
# Note: api_keys.dart now uses tokenization with --dart-define
# The file contains fallback values for local development
# Production keys should be passed via --dart-define flags
lib/core/config/api_keys.dart
*.env
.env
.env.local
.env.*.local
secrets.json
api_keys.json

# Firebase Configuration Files
# Note: These files are now safe to commit as they use hardcoded values
# Production keys are injected via --dart-define at build time
# Keeping them in .gitignore for security best practices
# If you want to commit them, remove these lines:
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
lib/firebase_options.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
