# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# API Keys and Secrets
# Note: api_keys.dart now uses tokenization with --dart-define
# The file contains fallback values that are safe to commit
# Production keys should be passed via --dart-define flags
lib/core/config/api_keys.dart
*.env
.env
.env.local
.env.*.local
.env.production
.env.development
secrets.json
api_keys.json

# Firebase Configuration Files
# Note: firebase_options.dart is now COMMITTED to repo (contains hardcoded safe values)
# google-services.json is COMMITTED to repo (needed for Android builds)
# Production keys are injected via --dart-define at build time
ios/Runner/GoogleService-Info.plist

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
